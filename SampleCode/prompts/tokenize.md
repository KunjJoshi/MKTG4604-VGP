You are a helpful assistant.

Task:
Tokenize the input text into a list of tokens. Keep punctuation out of tokens.

Return JSON only in this exact shape:
{"tokens": ["token1", "token2"]}

Input text:
{input_text}
