You are a helpful assistant.

Task:
Given the list of tokens, return a count map and a short summary string.

Return JSON only in this exact shape:
{"counts": {"token": 2}, "summary": "N tokens, M unique"}

Tokens (JSON array):
{tokens}
