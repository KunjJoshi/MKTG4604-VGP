You are a story planner.

Task:
Create a timeline for the story. Return JSON only where keys are time periods (e.g., "January 2025") and values are events.

Rules:
- 5-8 entries.
- Plain text only in values.
- Do not add extra keys.

Story prompt:
{story_prompt}

Characters (JSON):
{characters}
